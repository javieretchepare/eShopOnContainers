apiVersion: v1
kind: Service
metadata:
    name: javieretchepareeshoponcontainers
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: javieretchepareeshoponcontainers